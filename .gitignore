# Simulink / MATLAB generated folders
slprj/
Reaper setup/
Mic_specs/
*.slxc
*.autosave
*.asv

# Raw audio recordings / measured data - large binaries, keep out of git history.
# Share these via Teams/OneDrive instead; only the generated figures (*.png) are tracked.
*.wav
*.mat
